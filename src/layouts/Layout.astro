---
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  article?: boolean;
  publishDate?: string;
  modifiedDate?: string;
  eventTimelineScroll?: boolean;
}
const {
  title,
  description,
  ogImage,
  article = false,
  publishDate,
  modifiedDate,
  eventTimelineScroll = false,
}: Props = Astro.props;

import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

import "../global.css";

const canonicalURL = new URL(Astro.url.pathname, Astro.site)
  .toString()
  .replace(/\/$/, "");

// Absolute asset URLs derived from `site` in astro.config.mjs (single source of truth).
// OG/Twitter/JSON-LD images must be absolute — external scrapers won't resolve relative paths.
const origin = new URL("/", Astro.site).origin;
const logoURL = new URL("/assets/bliss-logo.png", Astro.site).href;
const bannerURL = new URL("/assets/bliss-banner.png", Astro.site).href;
const ogImageURL = new URL(ogImage ?? "/assets/bliss-banner.png", Astro.site).href;

const siteTitle = "BLISS - Berlin Learning & Intelligent Systems Society";
const fullTitle = title === siteTitle ? title : `${title} | BLISS Berlin`;

// Current date in ISO format for freshness indication
const currentDate = new Date().toISOString();

// Canonical @id anchors used to link all schemas together
const orgId = `${origin}/#organization`;
const siteId = `${origin}/#website`;

// JSON-LD: merged Organization + EducationalOrganization (canonical entity)
const orgSchema = JSON.stringify({
  "@context": "https://schema.org",
  "@type": ["Organization", "EducationalOrganization"],
  "@id": orgId,
  "name": "BLISS e.V.",
  "alternateName": "BLISS Berlin",
  "legalName": "Berlin Learning & Intelligent Systems Society e.V.",
  "url": origin,
  "foundingDate": "2022",
  "logo": {
    "@type": "ImageObject",
    "url": logoURL
  },
  "image": bannerURL,
  "description": "Berlin's research-focused AI non-profit organization connecting passionate individuals in machine learning through weekly reading groups, speaker and diversity events, and hackathons.",
  "disambiguatingDescription": "Non-profit AI research community (registered association / e.V.) in Berlin, running an academic AI speaker series, machine-learning paper reading groups, and hackathons. Not affiliated with any commercial or retail brand of a similar name.",
  "knowsAbout": [
    "Artificial Intelligence",
    "Machine Learning",
    "Deep Learning",
    "AI Research",
    "Natural Language Processing",
    "Computer Vision",
    "Reinforcement Learning",
    "AI Safety"
  ],
  "keywords": "artificial intelligence, machine learning, AI research, deep learning, Berlin AI community, AI speaker series, paper reading group, hackathon",
  "areaServed": {
    "@type": "City",
    "name": "Berlin"
  },
  "address": {
    "@type": "PostalAddress",
    "addressLocality": "Berlin",
    "addressRegion": "Berlin",
    "addressCountry": "DE"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": "52.5200",
    "longitude": "13.4050"
  },
  "sameAs": [
    "https://github.com/Bliss-e-V",
    "https://www.linkedin.com/company/bliss-berlin/",
    "https://www.meetup.com/bliss-speaker-series/",
    "https://www.youtube.com/@bliss.ev.berlin",
    "https://www.instagram.com/blissaicommunity",
    "https://discord.gg/QrbZtHb3sK"
  ]
});

// JSON-LD: WebSite (references Organization via @id)
const websiteSchema = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "WebSite",
  "@id": siteId,
  "name": "BLISS e.V.",
  "url": origin,
  "publisher": { "@id": orgId }
});

// JSON-LD: WebPage (references WebSite and Organization via @id)
const webpageSchema = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "WebPage",
  "@id": `${canonicalURL}#webpage`,
  "url": canonicalURL,
  "name": fullTitle,
  "description": description,
  "dateModified": currentDate,
  "isPartOf": { "@id": siteId },
  "about": { "@id": orgId }
});

// JSON-LD: Article (only emitted on article pages)
const articleSchema = article && publishDate
  ? JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Article",
      "headline": title,
      "description": description,
      "image": ogImageURL,
      "datePublished": publishDate,
      "dateModified": modifiedDate || currentDate,
      "publisher": { "@id": orgId },
      "mainEntityOfPage": { "@id": `${canonicalURL}#webpage` }
    })
  : null;
---

<!doctype html>
<html
  lang="en"
  class:list={{
    "event-timeline-scroll-pending": eventTimelineScroll,
    "event-timeline-page": eventTimelineScroll,
  }}
>
  <head>
    <title>{fullTitle}</title>
    <link rel="canonical" href={canonicalURL} />
    <meta name="description" content={description} />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <meta name="google-site-verification" content="_xg6ClW404XDoyS14BrmvIDAmENsoQgsTfTXc4Of2jU" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content={article ? "article" : "website"} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageURL} />
    <meta property="og:site_name" content="BLISS Berlin" />
    {article && publishDate && <meta property="article:published_time" content={publishDate} />}
    {article && modifiedDate && <meta property="article:modified_time" content={modifiedDate} />}

    <!-- Twitter (X) -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:url" content={canonicalURL} />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageURL} />

    <!-- Keywords -->
    <meta name="keywords" content="ai, machine learning, artificial intelligence, events, berlin, speaker series, tech community, bliss berlin, data science, deep learning" />

    <!-- Theme and icons -->
    <meta name="theme-color" content="#000000" />
    <link rel="icon" href="/favicon.ico" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#000000" />
    <link rel="manifest" href="/site.webmanifest" />

    <!-- Sitemap -->
    <link rel="sitemap" type="application/xml" title="Sitemap" href="/sitemap-index.xml" />

    <!-- Structured data: Organization + EducationalOrganization (canonical entity) -->
    <script type="application/ld+json" set:html={orgSchema}></script>

    <!-- Structured data: WebSite -->
    <script type="application/ld+json" set:html={websiteSchema}></script>

    <!-- Structured data: WebPage -->
    <script type="application/ld+json" set:html={webpageSchema}></script>

    <!-- Structured data: Article (article pages only) -->
    {articleSchema && <script type="application/ld+json" set:html={articleSchema}></script>}
  </head>

  <body>
    <Header compact={eventTimelineScroll} />
    <div
      class:list={[
        "container flex-1",
        eventTimelineScroll ? "pt-20" : "pt-40",
      ]}
    >
      <slot />
    </div>
    <Footer />
  </body>
</html>

<script>
  // enable web analytics via vercel (deferred to avoid main-thread work during first paint)
  import { inject } from "@vercel/analytics";

  // @ts-ignore
  globalThis.process = { env: { NODE_ENV: import.meta.env.MODE } };
  const run = () => inject();
  if ("requestIdleCallback" in window) {
    requestIdleCallback(run, { timeout: 2500 });
  } else {
    window.addEventListener("load", run, { once: true });
  }
</script>

<style is:global>
  @reference "../global.css";

  :root {
    --accent: #cb2d3e;
    --accent-dark: #3c0909;
    --accent-light: #ef473a;
    --background: #000000;
    --background-secondary: #101010;
    --primary: #ffffff;
    --secondary: #adb5bd;
    --tertiary: #77808d;
    --border: #3b424d;
  }

  ::selection {
    background: var(--accent);
    color: white;
  }

  html {
    background-color: var(--background);
  }

  body {
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 100vh;
    color: var(--primary);
  }

  a {
    text-decoration: underline;
    text-decoration-color: transparent;
    text-underline-offset: 4px;
    transition: text-decoration-color 300ms;
  }

  a:hover {
    text-decoration-color: var(--color);
  }

  .no-underline:hover {
    text-decoration-color: transparent !important;
  }

  .container {
    width: 100%;
    max-width: 80rem;
    padding-left: 20px;
    padding-right: 20px;
    margin: 0 auto;
  }

  .full-width {
    width: 100vw;
    position: relative;
    left: 50%;
    right: 50%;
    margin-left: -50vw;
    margin-right: -50vw;
  }

  article h1,
  article h2,
  article h3 {
    @apply text-primary;
  }
  article h1 {
    @apply text-3xl sm:text-5xl;
  }

  article h2 {
    @apply text-2xl sm:text-4xl;
  }

  article h3 {
    @apply text-xl sm:text-2xl;
  }

  article p {
    @apply my-1 text-justify;
  }

  article p,
  article li {
    @apply text-secondary text-lg font-light sm:leading-relaxed sm:text-xl;
  }
  article hr {
    @apply border-t-[1px] border-border mb-2;
  }

  article strong {
    @apply text-primary font-normal;
  }

  article blockquote {
    @apply bg-background-secondary border-l-4 border-accent px-3 py-2 my-2;
  }

  article p > code {
    @apply text-[85%] text-accent bg-accent-dark rounded-lg p-1;
  }

  article pre {
    @apply p-4 my-4 rounded-lg;
  }

  article ol,
  article ul {
    @apply my-2;
  }
  article ul li {
    @apply list-item list-disc ml-[30px];
  }

  article ol li {
    @apply list-item list-decimal ml-[30px];
  }

  article h2,
  article h3 {
    @apply mt-8 mb-2;
  }
  article a {
    color: #9a4fba;
  }
  article img {
    @apply mx-auto my-4 rounded-lg;
  }

  .excalidraw {
    filter: invert(100%) hue-rotate(180deg);
  }
</style>
